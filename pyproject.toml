[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "challenge-spiro"
version = "0.0.1"
dependencies = [
  'numpy==2.2.6',
  'dcmri < 0.7.0', 
  'pydmr',
  'tqdm',
  'pandas',
]

[tool.setuptools.packages.find]
where = ["src"]
